# mlr3summary (development version)

* `summary()` with `importance_measures = "shap"` now requires `fastshap` to be installed from r-universe (`https://bgreenwell.r-universe.dev`), since the package was archived on CRAN.

# mlr3summary 0.1.2

* resolve issue if `task$properties` is a vector
* compatibility: mlr3 1.4.0

# mlr3summary 0.1.0

* Initial release

# mlr3summary 0.1.1

* Add missing suggests
* Resolve error with integer transformation
* Add CITATION
